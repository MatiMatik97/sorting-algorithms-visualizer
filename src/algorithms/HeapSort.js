import { DONE_TIME, swapInArray, finishAnimations } from '../helpers';

import { HEAP_SORT_COLORS } from '../helpers';

const { BARS_BASIC_BG_COLOR,
    BARS_SWAPPED_BG_COLOR,
    BARS_SWAP_BG_COLOR } = HEAP_SORT_COLORS;

const HeapSort = () => { }

HeapSort.init = (bars, setBars, stepTime, sortingOrder) => {
    console.log("HeapSort");

    return 0;
}

export default HeapSort